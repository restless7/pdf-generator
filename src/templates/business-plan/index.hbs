<!DOCTYPE html>
<html lang="{{_generation.locale}}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{businessName}} - Plan de Negocio</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            line-height: 1.6;
            color: #2d3748;
            background: #fff;
        }
        
        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 40px 20px;
        }
        
        /* Header Styles */
        .header {
            text-align: center;
            margin-bottom: 60px;
            padding: 40px 0;
            border-bottom: 3px solid {{default _branding.colors.primary '#3182ce'}};
        }
        
        .logo {
            max-width: 200px;
            max-height: 80px;
            margin-bottom: 20px;
        }
        
        .company-name {
            font-size: 2.5rem;
            font-weight: 700;
            color: {{default _branding.colors.primary '#3182ce'}};
            margin-bottom: 10px;
        }
        
        .document-title {
            font-size: 1.8rem;
            color: #4a5568;
            font-weight: 300;
        }
        
        .generation-info {
            margin-top: 20px;
            font-size: 0.9rem;
            color: #718096;
        }
        
        /* Section Styles */
        .section {
            margin-bottom: 50px;
            page-break-inside: avoid;
        }
        
        .section-title {
            font-size: 1.8rem;
            font-weight: 600;
            color: {{default _branding.colors.primary '#3182ce'}};
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 2px solid {{default _branding.colors.secondary '#e2e8f0'}};
        }
        
        .section-content {
            font-size: 1rem;
            line-height: 1.7;
            text-align: justify;
        }
        
        /* Business Info Styles */
        .business-info {
            background: #f7fafc;
            padding: 30px;
            border-radius: 8px;
            margin-bottom: 40px;
        }
        
        .info-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin-top: 20px;
        }
        
        .info-item {
            padding: 15px;
            background: white;
            border-radius: 6px;
            border-left: 4px solid {{default _branding.colors.primary '#3182ce'}};
        }
        
        .info-label {
            font-weight: 600;
            color: #4a5568;
            font-size: 0.9rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        
        .info-value {
            font-size: 1.1rem;
            color: #2d3748;
            margin-top: 5px;
        }
        
        /* Highlights Styles */
        .highlights {
            background: linear-gradient(135deg, {{default _branding.colors.primary '#3182ce'}}10, {{default _branding.colors.secondary '#e2e8f0'}}10);
            padding: 25px;
            border-radius: 8px;
            margin: 20px 0;
            border-left: 5px solid {{default _branding.colors.primary '#3182ce'}};
        }
        
        .highlights h4 {
            color: {{default _branding.colors.primary '#3182ce'}};
            font-size: 1.2rem;
            margin-bottom: 15px;
        }
        
        .highlights p {
            font-size: 1rem;
            line-height: 1.6;
        }
        
        /* Table of Contents */
        .toc {
            background: #f8f9fa;
            padding: 30px;
            border-radius: 8px;
            margin-bottom: 40px;
        }
        
        .toc h3 {
            color: {{default _branding.colors.primary '#3182ce'}};
            margin-bottom: 20px;
            font-size: 1.5rem;
        }
        
        .toc-list {
            list-style: none;
            padding: 0;
        }
        
        .toc-item {
            padding: 8px 0;
            border-bottom: 1px dotted #cbd5e0;
            display: flex;
            justify-content: space-between;
        }
        
        .toc-item:last-child {
            border-bottom: none;
        }
        
        .toc-title {
            font-weight: 500;
        }
        
        .toc-page {
            color: #718096;
        }
        
        /* Page Break Utilities */
        .page-break {
            page-break-before: always;
        }
        
        .avoid-break {
            page-break-inside: avoid;
        }
        
        /* Footer */
        .document-footer {
            margin-top: 60px;
            padding-top: 30px;
            border-top: 2px solid #e2e8f0;
            text-align: center;
            color: #718096;
            font-size: 0.9rem;
        }
        
        /* Print Styles */
        @media print {
            body {
                font-size: 11pt;
            }
            
            .container {
                padding: 20px;
            }
            
            .section {
                margin-bottom: 30px;
            }
            
            .page-break {
                page-break-before: always;
            }
        }
        
        /* Quality-specific styles */
        {{#if (eq _generation.quality 'high')}}
        .section-content {
            text-rendering: optimizeLegibility;
            -webkit-font-smoothing: antialiased;
        }
        {{/if}}
        
        {{#if (eq _generation.quality 'draft')}}
        .highlights,
        .business-info,
        .toc {
            box-shadow: none;
            border: 1px solid #e2e8f0;
        }
        {{/if}}
    </style>
</head>
<body>
    <div class="container">
        <!-- Header Section -->
        <header class="header">
            {{#if _assets.logos.apex-logo}}
                {{#if _assets.logos.apex-logo.variants.color}}
                    <img src="{{_assets.logos.apex-logo.variants.color}}" alt="{{default _branding.companyName 'Apex Agency'}} Logo" class="logo">
                {{else if _assets.logos.apex-logo.main}}
                    <img src="{{_assets.logos.apex-logo.main}}" alt="{{default _branding.companyName 'Apex Agency'}} Logo" class="logo">
                {{/if}}
            {{/if}}
            
            <h1 class="company-name">{{businessName}}</h1>
            <h2 class="document-title">Plan de Negocio</h2>
            
            <div class="generation-info">
                <p>Generado el {{formatDate generationDate 'long'}}</p>
                {{#if metadata.plan}}
                    <p>Plan: {{uppercase metadata.plan}}</p>
                {{/if}}
            </div>
        </header>

        <!-- Business Information -->
        <section class="business-info avoid-break">
            <h3 class="section-title">Información de la Empresa</h3>
            
            <div class="info-grid">
                <div class="info-item">
                    <div class="info-label">Nombre de la Empresa</div>
                    <div class="info-value">{{businessName}}</div>
                </div>
                
                <div class="info-item">
                    <div class="info-label">Tipo de Negocio</div>
                    <div class="info-value">{{businessType}}</div>
                </div>
                
                <div class="info-item">
                    <div class="info-label">Ubicación</div>
                    <div class="info-value">{{location}}</div>
                </div>
                
                <div class="info-item">
                    <div class="info-label">Público Objetivo</div>
                    <div class="info-value">{{targetAudience}}</div>
                </div>
            </div>
            
            {{#if businessDescription}}
            <div class="section-content" style="margin-top: 30px;">
                <h4 style="color: {{default _branding.colors.primary '#3182ce'}}; margin-bottom: 15px;">Descripción del Negocio</h4>
                <p>{{businessDescription}}</p>
            </div>
            {{/if}}
        </section>

        <!-- Table of Contents -->
        {{#if (gt (length sections) 3)}}
        <section class="toc avoid-break">
            <h3>Índice de Contenidos</h3>
            <ul class="toc-list">
                {{#each sections}}
                <li class="toc-item">
                    <span class="toc-title">{{@index}}.{{add @index 1}} {{title}}</span>
                    <span class="toc-page">{{add @index 2}}</span>
                </li>
                {{/each}}
            </ul>
        </section>
        {{/if}}

        <!-- Sections -->
        {{#each sections}}
        <section class="section {{#if (gt @index 2)}}page-break{{/if}}">
            <h3 class="section-title">{{add @index 1}}. {{title}}</h3>
            
            <div class="section-content">
                {{{content}}}
            </div>
            
            {{#if highlights}}
            <div class="highlights">
                <h4>Puntos Destacados</h4>
                <p>{{{highlights}}}</p>
            </div>
            {{/if}}
        </section>
        {{/each}}

        <!-- Document Footer -->
        <footer class="document-footer">
            <p>
                Documento generado por {{default _branding.companyName 'Apex Agency'}} 
                el {{formatDate _generation.date}} - Versión {{_template.version}}
            </p>
            {{#if (neq _generation.quality 'draft')}}
            <p style="margin-top: 10px; font-size: 0.8rem;">
                Este documento es confidencial y está destinado únicamente al uso interno de {{businessName}}.
            </p>
            {{/if}}
        </footer>
    </div>
</body>
</html>
