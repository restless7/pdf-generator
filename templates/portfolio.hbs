<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            line-height: 1.6;
            color: #1a1a1a;
            background: #ffffff;
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 40px;
        }

        /* Header Section */
        .header {
            text-align: center;
            margin-bottom: 60px;
            padding-bottom: 30px;
            border-bottom: 2px solid #007bff;
        }

        .logo-space {
            width: 150px;
            height: 60px;
            margin: 0 auto 30px;
            border: 2px dashed #e0e0e0;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #999;
            font-size: 12px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .company-name {
            font-size: 36px;
            font-weight: bold;
            color: #007bff;
            margin-bottom: 10px;
            letter-spacing: -0.5px;
        }

        .company-tagline {
            font-size: 18px;
            color: #666;
            font-weight: 300;
        }

        /* Table of Contents */
        .toc {
            background: #f8f9fa;
            padding: 30px;
            margin-bottom: 50px;
            border-radius: 8px;
        }

        .toc h2 {
            font-size: 24px;
            color: #1a1a1a;
            margin-bottom: 20px;
            text-align: center;
        }

        .toc-list {
            list-style: none;
            columns: 2;
            column-gap: 40px;
        }

        .toc-item {
            margin-bottom: 8px;
            break-inside: avoid;
        }

        .toc-item a {
            text-decoration: none;
            color: #007bff;
            font-weight: 500;
            display: block;
            padding: 5px 0;
            transition: color 0.2s ease;
        }

        .toc-item a:hover {
            color: #0056b3;
        }

        /* Service Sections */
        .service {
            margin-bottom: 50px;
            page-break-inside: avoid;
        }

        .service-header {
            margin-bottom: 25px;
            padding-bottom: 15px;
            border-bottom: 1px solid #e0e0e0;
        }

        .service-title {
            font-size: 28px;
            color: #007bff;
            margin-bottom: 8px;
            font-weight: bold;
        }

        .service-focus {
            font-size: 16px;
            color: #666;
            font-style: italic;
        }

        .service-section {
            margin-bottom: 25px;
        }

        .section-title {
            font-size: 18px;
            color: #1a1a1a;
            margin-bottom: 15px;
            font-weight: 600;
            display: flex;
            align-items: center;
        }

        .section-icon {
            width: 4px;
            height: 18px;
            background: #007bff;
            margin-right: 12px;
        }

        .features-list, .tech-list, .process-list {
            list-style: none;
        }

        .feature-item, .tech-item, .process-item {
            margin-bottom: 12px;
            padding-left: 20px;
            position: relative;
        }

        .feature-item::before {
            content: "•";
            color: #007bff;
            font-weight: bold;
            position: absolute;
            left: 0;
            font-size: 16px;
        }

        .process-item {
            background: #f8f9fa;
            padding: 15px 20px;
            border-radius: 6px;
            margin-bottom: 15px;
            border-left: 4px solid #007bff;
        }

        .process-item::before {
            content: none;
        }

        .process-number {
            font-weight: bold;
            color: #007bff;
            display: inline-block;
            margin-right: 8px;
        }

        .feature-title {
            font-weight: 600;
            color: #1a1a1a;
        }

        .feature-description, .process-description {
            color: #666;
            margin-top: 4px;
        }

        .tech-stack {
            background: #f0f8ff;
            padding: 20px;
            border-radius: 6px;
            border-left: 4px solid #007bff;
        }

        .tech-description {
            color: #444;
            font-size: 15px;
        }

        /* Competitive Advantages */
        .advantages {
            background: linear-gradient(135deg, #007bff 0%, #0056b3 100%);
            color: white;
            padding: 40px;
            border-radius: 12px;
            margin-top: 60px;
        }

        .advantages h2 {
            font-size: 32px;
            text-align: center;
            margin-bottom: 30px;
            font-weight: bold;
        }

        .advantages-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 20px;
        }

        .advantage-item {
            display: flex;
            align-items: flex-start;
            padding: 15px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 8px;
        }

        .advantage-icon {
            color: #28a745;
            margin-right: 12px;
            font-size: 18px;
            margin-top: 2px;
        }

        .advantage-content {
            flex: 1;
        }

        .advantage-title {
            font-weight: 600;
            margin-bottom: 4px;
        }

        .advantage-description {
            font-size: 14px;
            opacity: 0.9;
        }

        /* Footer */
        .footer {
            text-align: center;
            margin-top: 60px;
            padding-top: 30px;
            border-top: 1px solid #e0e0e0;
            color: #666;
            font-size: 14px;
        }

        /* Print Styles */
        @media print {
            .container {
                padding: 20px;
            }
            
            .service {
                page-break-inside: avoid;
                margin-bottom: 40px;
            }
            
            .toc-list {
                columns: 1;
            }
            
            .advantages-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Header -->
        <div class="header">
            <div class="logo-space">
                LOGO PLACEHOLDER
            </div>
            <h1 class="company-name">{{companyName}}</h1>
            <p class="company-tagline">{{tagline}}</p>
        </div>

        <!-- Table of Contents -->
        <div class="toc">
            <h2>Service Portfolio Overview</h2>
            <ul class="toc-list">
                {{#each services}}
                <li class="toc-item">
                    <a href="#service-{{@index}}">{{title}}</a>
                </li>
                {{/each}}
            </ul>
        </div>

        <!-- Services -->
        {{#each services}}
        <div class="service" id="service-{{@index}}">
            <div class="service-header">
                <h2 class="service-title">{{title}}</h2>
                <p class="service-focus">{{coreDescription}}</p>
            </div>

            {{#if features}}
            <div class="service-section">
                <h3 class="section-title">
                    <span class="section-icon"></span>
                    Key Features & Benefits
                </h3>
                <ul class="features-list">
                    {{#each features}}
                    <li class="feature-item">
                        <span class="feature-title">{{title}}:</span>
                        <div class="feature-description">{{description}}</div>
                    </li>
                    {{/each}}
                </ul>
            </div>
            {{/if}}

            {{#if technologyStack}}
            <div class="service-section">
                <h3 class="section-title">
                    <span class="section-icon"></span>
                    Technology Stack
                </h3>
                <div class="tech-stack">
                    <p class="tech-description">{{technologyStack}}</p>
                </div>
            </div>
            {{/if}}

            {{#if implementationProcess}}
            <div class="service-section">
                <h3 class="section-title">
                    <span class="section-icon"></span>
                    Implementation Process
                </h3>
                <ul class="process-list">
                    {{#each implementationProcess}}
                    <li class="process-item">
                        <span class="process-number">{{step}}.</span>
                        <strong>{{title}}</strong> - {{description}}
                    </li>
                    {{/each}}
                </ul>
            </div>
            {{/if}}
        </div>
        {{/each}}

        <!-- Competitive Advantages -->
        {{#if competitiveAdvantages}}
        <div class="advantages">
            <h2>Competitive Advantages Across All Services</h2>
            <div class="advantages-grid">
                {{#each competitiveAdvantages}}
                <div class="advantage-item">
                    <span class="advantage-icon">✓</span>
                    <div class="advantage-content">
                        <div class="advantage-title">{{title}}</div>
                        <div class="advantage-description">{{description}}</div>
                    </div>
                </div>
                {{/each}}
            </div>
        </div>
        {{/if}}

        <!-- Footer -->
        <div class="footer">
            <p>© {{year}} {{companyName}} - Intelligent Solutions for Tomorrow's Challenges</p>
        </div>
    </div>
</body>
</html>
